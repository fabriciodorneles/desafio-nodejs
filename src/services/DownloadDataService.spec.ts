import DownloadDataService from './DownloadDataService';
import CrudeDataRepository from '../repositories/CrudeDataRepository';

describe('DownloadData', () => {
  it('should be able to get data from API', () => {
    expect(1 + 2).toBe(3);
  });
  it('should be able to call initialRepo with data downloaded', () => {
    expect(1 + 2).toBe(3);
  });
});
